/******
 * SanitizationMiddleware
 *
 * Sanitizes the text.
 ******/
import { IWebChatAction } from "../../../interfaces/IWebChatAction";
declare const sanitizationMiddleware: ({ dispatch }: {
    dispatch: any;
}) => (next: any) => (action: IWebChatAction) => any;
export default sanitizationMiddleware;
