import * as React from "react";
import { DefaultButton, PrimaryButton } from "@fluentui/react";
const iconButtonStyles = {
  root: {
    margin: 0
  }
};
export const defaultFooterComponentOverrides = {
  DownloadTranscriptButton: /*#__PURE__*/React.createElement(PrimaryButton, {
    text: "Download",
    onClick: _downloadClicked,
    allowDisabledFocus: true,
    styles: iconButtonStyles
  }),
  EmailTranscriptButton: /*#__PURE__*/React.createElement(DefaultButton, {
    text: "Email",
    onClick: _emailClicked,
    allowDisabledFocus: true,
    styles: iconButtonStyles
  })
};

function _downloadClicked() {
  console.log("download clicked");
}

function _emailClicked() {
  console.log("email clicked");
}