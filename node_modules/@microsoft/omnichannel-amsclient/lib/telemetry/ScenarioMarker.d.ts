import AMSLogger from "../AMSLogger";
declare class ScenarioMarker {
    private runtimeId;
    private sdkVersion;
    private telemetryEvents;
    private logger;
    constructor(logger: AMSLogger);
    setRuntimeId(runtimeId: string): void;
    startScenario(event: string, additionalProperties?: {}): void;
    failScenario(event: string, additionalProperties?: {}): void;
    completeScenario(event: string, additionalProperties?: {}): void;
}
export default ScenarioMarker;
