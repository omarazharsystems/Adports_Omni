import SDKProvider from "./SDKProvider";
import { uuidv4 } from "./Utils/uuid";
declare global {
    interface Window {
        Microsoft: any;
    }
    namespace NodeJS {
        interface Global {
            window: any;
        }
    }
}
export { SDKProvider, uuidv4 };
