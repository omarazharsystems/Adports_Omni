import IOCSDKLogger from "../Interfaces/IOCSDKLogger";
import ILogger from "../Model/ILogger";
import IOCSDKLogData from "../Model/IOCSDKLogData";
import { LogLevel } from "../Model/LogLevel";
export default class OCSDKLogger implements IOCSDKLogger {
    private logger;
    constructor(logger: ILogger);
    log(loglevel: LogLevel, telemetryEvent: string, customData?: any, description?: string): void;
    logEvent(logLevel: LogLevel, logData: IOCSDKLogData): number | void;
    isLoggingEnabled(): boolean;
    isNullOrUndefined(obj: object): boolean;
}
